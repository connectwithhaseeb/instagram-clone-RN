import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container:{
        marginBottom:8,
    },
    'image':{
        width: '100%',
        height: 450,
    }
});

export default styles;