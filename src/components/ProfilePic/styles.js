import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    'container':{
        borderWidth: 3,
        borderRadius: 40,
        margin:8,
        borderColor: '#c06eff'
    },
    'image':{
        borderRadius: 40,
        borderWidth: 1,
        borderColor:'#fff'
    }
})

export default styles;